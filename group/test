DNS查询
发起握手，又客户端发起带有SYN请求同步的消息，服务端返回ACK和SYN同步消息，并且都带有seq序列号和ack确认下一轮的序列号。再由客户端ACK进行确认。
主要作用就是同步seq序列号，保证在全局时间作用域下是唯一的，达到可靠的传输
传输数据。
http应用层  表示层 会话层 tcp传输层 ip网络层 数据链路层 物理层
tcp切割合成数据
ssl、tls 加解密数据、传递数字证书、校验消息摘要(完整性校验)、CA机构校验身份
html -> 数据流 根据编码 -> 字符码（写的代码）通过词法分析、语法分析 -> 字节token 解析为字节对象 -> DOM树
解析css -> 样式规则
DOM树根据样式规则得到layout布局树 -> 规定DOM如何从上到下排列
再进行painting绘制 -> 发送给GPU进行合成
如果是

  s   e  a
e se  s sa
a eas sa s
t seat 